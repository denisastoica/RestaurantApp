<Page x:Class="Restaurant.Views.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:services="clr-namespace:Restaurant.Services"
      Title="Comenzile mele">

    <Page.Resources>
        <services:StatusToBoolConverter x:Key="StatusToBoolConverter"/>
    </Page.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Reîmprospătează"
                Command="{Binding RefreshCommand}"
                Width="120"
                Margin="0,0,0,10"
                Grid.Row="0"/>

        <ListView ItemsSource="{Binding Orders}" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" BorderBrush="Gray" Padding="5" Margin="2">
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <TextBlock Text="Cod: " FontWeight="Bold"/>
                                <TextBlock Text="{Binding Code}" Width="140"/>
                                <TextBlock Text="Data: " FontWeight="Bold" Margin="10,0,0,0"/>
                                <TextBlock Text="{Binding Date, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                                <TextBlock Text="Status: " FontWeight="Bold" Margin="10,0,0,0"/>
                                <TextBlock Text="{Binding Status.Status}" Width="100"/>
                                <TextBlock Text="Livrare estimată:" FontWeight="Bold"/>
                                <TextBlock Text="{Binding DeliveryEta, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" Foreground="RoyalBlue"/>


                            </StackPanel>

                            <TextBlock Text="Produse:" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Items}" TextWrapping="Wrap" Margin="5,0,0,5"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <TextBlock Text="Total: " FontWeight="Bold"/>
                                <TextBlock Text="{Binding Total, StringFormat='{}{0:F2} lei'}" Foreground="DarkGreen"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Anulează"
                                        Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding OrderId}"
                                        IsEnabled="{Binding StatusText, Converter={StaticResource StatusToBoolConverter}}"
                                        Width="75"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
